---
/** Visible placeholder for an unconfirmed owner fact. Grep for "TODO" before launch. */
interface Props { label: string }
const { label } = Astro.props;
---
<span class="todo" data-todo>TODO: {label}</span>
